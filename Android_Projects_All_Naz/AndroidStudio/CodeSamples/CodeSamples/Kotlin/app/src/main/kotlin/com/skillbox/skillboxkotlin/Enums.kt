package com.skillbox.skillboxkotlin


fun main() {

//    listOf<Drawable>(Color.BLACK, Color.BLUE).forEach { it.draw() }
//
//    Color.valueOf("blackh".toUpperCase()).draw()

    Color.values()[Color.RED.ordinal]
    Color.RED.ordinal
    Color.RED.name
}
